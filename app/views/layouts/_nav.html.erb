<nav>
  <div class="nav-inner">
    <%= link_to quizzes_path, title: "Home", class: "nav-link" do %>
      <h1 class="link-lg"><%= fa_icon "home", class: "fa-lg", id: "home-icon" %><span class="nav-link-heading">Briz Quiz List</span></h1>
    <% end %>
    <%= link_to "/map", title: "Map", class: "nav-link#{" active" if current_page?("/map")}" do %>
      <div><%= fa_icon "map", class: "fa-lg" %><span class="nav-link-heading">Map</span></div>
    <% end %>
    <% if user_signed_in? %>
      <%= link_to calendar_path, title: "Calendar", class: "nav-link#{" active" if current_page?("/calendar")}" do %>
        <div><%= fa_icon "calendar-check-o", class: "fa-lg" %><span class="nav-link-heading">Calendar</span></div>
      <% end %>
      <%= link_to favourites_path, title: "Faves", class: "nav-link#{" active" if current_page?("/favourites")}" do %>
        <div><%= fa_icon "heart", class: "fa-lg" %><span class="nav-link-heading">Faves</span></div>
      <% end %>
    <% end %>
    <div class="account-container">
      <div class="account-link">
        <% if user_signed_in? %>
          <%= avatar_for(current_user) %>
        <% else %>
          <%= fa_icon "user-circle", class: "fa-lg" %>
        <% end %>
        <%= fa_icon "chevron-down", class: "fa-sm" %>
      </div>
      <div class="account-menu">
        <div class="account-menu-marker"></div>
        <div class="account-menu-list">
          <% if user_signed_in? %>
            <%= link_to edit_user_registration_path do %>
              <div class="icon-link"><%= fa_icon "user" %> <%= truncate(current_user.username || current_user.email, length: 25) %></div>
            <% end %>
            <%= link_to my_quizzes_path do %>
              <div class="icon-link"><%= fa_icon "home" %> My Quizzes</div>
            <% end %>
            <%= link_to new_quiz_path do %>
              <div class="icon-link"><%= fa_icon "plus" %> New quiz</div>
            <% end %>
            <%= link_to destroy_user_session_path, method: :delete do %>
              <div class="icon-link"><%= fa_icon "sign-out" %> Log out</div>
            <% end %>
          <% else %>
            <%= link_to new_user_session_path do %>
              <div class="icon-link"><%= fa_icon "sign-in" %> Log in</div>
            <% end %>
            <%= link_to new_user_registration_path do %>
              <div class="icon-link"><%= fa_icon "user-plus" %> Sign up</div>
            <% end %>
          <% end %>
        </div>
      </div>
      </div>
  </div>
</nav>
